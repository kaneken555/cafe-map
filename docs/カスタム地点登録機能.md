# カスタム地点登録機能 仕様書

## 1. 機能の目的・概要

カフェマップアプリケーションにおいて、ユーザーが**カフェ以外の独自の地点**をマップ上に登録できる機能。Google Places APIに依存せず、ユーザーが自由に地点を追加・管理できるようにする。

### 主な機能

- **地点登録**: ユーザーがマップ上の任意の位置にカスタム地点を登録
- **情報管理**: 地点名、種類、メモ、画像などの情報を保存
- **マップ連携**: 登録した地点を複数のマイマップに追加可能

### 想定される用途

- 写真撮影スポット
- 待ち合わせ場所
- 景色の良い場所
- 記念碑・モニュメント
- その他、ユーザー独自の地点

### 設計思想

**既存機能との違い:**
- カフェ: Google Places APIから取得した公式データ
- カスタム地点: ユーザーが独自に登録・管理するデータ

**データ管理:**
- カフェとは異なる新しいモデル「CustomPlace」を作成
- ユーザーごとに**所有権を明確に管理**
- マップとの関連は「CustomPlaceMapRelation」で管理

---

## 2. データモデル

### 2-1. CustomPlace（カスタム地点）

#### モデル定義

```python
class CustomPlace(models.Model):
    """ユーザーが独自に登録する地点"""

    # 基本情報
    owner = models.ForeignKey(
        User,
        on_delete=models.CASCADE,
        related_name='custom_places'
    )
    name = models.CharField(max_length=100)

    # 位置情報
    latitude = models.FloatField(
        validators=[MinValueValidator(-90), MaxValueValidator(90)]
    )
    longitude = models.FloatField(
        validators=[MinValueValidator(-180), MaxValueValidator(180)]
    )

    # 追加情報
    image = models.ImageField(
        upload_to='custom_places/',
        null=True,
        blank=True
    )
    place_type = models.CharField(
        max_length=50,
        null=True,
        blank=True,
        choices=[
            ('photo_spot', '写真スポット'),
            ('meeting_point', '待ち合わせ場所'),
            ('viewpoint', '景色の良い場所'),
            ('memorial', '記念碑・モニュメント'),
            ('other', 'その他'),
        ]
    )
    memo = models.TextField(null=True, blank=True)

    # タイムスタンプ
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        db_table = 'custom_place'
        ordering = ['-created_at']
        indexes = [
            models.Index(fields=['owner', '-created_at']),
            models.Index(fields=['latitude', 'longitude']),
        ]
```

**フィールド詳細:**

| フィールド | 型 | 必須 | 説明 |
|-----------|-----|------|------|
| `owner` | FK(User) | ○ | 登録者 |
| `name` | varchar(100) | ○ | 地点名 |
| `latitude` | float | ○ | 緯度（-90〜90） |
| `longitude` | float | ○ | 経度（-180〜180） |
| `image` | ImageField | × | 画像（5MB以下） |
| `place_type` | varchar(50) | × | 地点種別 |
| `memo` | text | × | メモ・説明 |
| `created_at` | datetime | auto | 作成日時 |
| `updated_at` | datetime | auto | 更新日時 |

---

### 2-2. CustomPlaceMapRelation（マップとの関連）

#### モデル定義

```python
class CustomPlaceMapRelation(models.Model):
    """カスタム地点とマップの関連"""

    map = models.ForeignKey(
        Map,
        on_delete=models.CASCADE,
        related_name='custom_place_relations'
    )
    custom_place = models.ForeignKey(
        CustomPlace,
        on_delete=models.CASCADE,
        related_name='map_relations'
    )

    # 表示設定
    is_visible = models.BooleanField(default=True)

    # タイムスタンプ
    created_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        db_table = 'custom_place_map_relation'
        unique_together = [['map', 'custom_place']]
        indexes = [
            models.Index(fields=['map', 'is_visible']),
        ]
```

**設計ポイント:**
- 既存の `CafeMapRelation` と同様の構造
- 1つのカスタム地点を複数マップで共有可能
- `is_visible` で個別マップでの表示ON/OFF制御

---

### ER図

```

      User
        │
          │1
         │
          │*
        ↓
   CustomPlace

 - owner_id (FK)                      Map
 - name                                 │
 - latitude                               │1
 - longitude                             │
 - image                                  │*
 - place_type                           ↓
 - memo                    1     CustomPlaceMapRelation
 - created_at                   ├────────────────────────┤
 - updated_at                *   - map_id (FK)
                                 - custom_place_id (FK)
                                   - is_visible
                                   - created_at

```

---

## 3. UI/UX設計

### 登録フロー

#### パターン1: マップをクリックして登録

```
1. マップ上の任意の地点をクリック or 長押し
   ↓
2. コンテキストメニューが表示
   → 「この場所にカスタム地点を追加」
   ↓
3. カスタム地点登録フォームが表示
   - 名前: 自動で「新しい地点」
   - 座標: クリックした位置を自動入力
   - その他: 空欄
   ↓
4. 必要情報を入力
   ↓
5. API呼び出し → カスタム地点作成 + マップへ関連付け
   ↓
6. 完了
→ マップに地点が表示
```

#### パターン2: 座標を直接入力して登録

```
1. ＋ボタン / カスタム地点追加メニューを選択
   ↓
2. カスタム地点登録フォームが表示
   - 名前: 入力必須
   - 緯度・経度: 手動入力 or 地図選択
   ↓
3. 保存ボタン → 確認
   ↓
4. 完了
→ マップに地点が表示
```

---

### 登録フォーム UI

#### 基本レイアウト

```
┌─────────────────────────────────────┐
│  カスタム地点の登録                 ×  │
├─────────────────────────────────────┤
│                                     │
│  名前 *                             │
│  ┌───────────────────────────────┐  │
│  │ 東京タワー                     │  │
│  └───────────────────────────────┘  │
│                                     │
│  位置情報 *                          │
│  ┌────┐         ┌────┐             │
│  │ 35.6890     │ 139.702  │        │
│  └────┘         └────┘              │
│  緯度           経度                 │
│  → 「地図で位置を選択」                │
│                                     │
│  画像                               │
│  ┌───────────────────────────────┐  │
│  │ [ファイル選択] または [URL入力]  │  │
│  └───────────────────────────────┘  │
│                                     │
│  種別                               │
│  ┌───────────────────────────────┐  │
│  │ ▼ 写真スポット                 │  │
│  └───────────────────────────────┘  │
│                                     │
│  メモ                               │
│  ┌───────────────────────────────┐  │
│  │ 夕日の時間帯が最も美しい          │  │
│  │                               │  │
│  │                               │  │
│  └───────────────────────────────┘  │
│                                     │
│  登録先マップ                         │
│  ┌───────────────────────────────┐  │
│  │ ☑ お気に入りカフェマップ         │  │
│  └───────────────────────────────┘  │
│                                     │
│       [キャンセル]    [保存]          │
└─────────────────────────────────────┘
```

#### フィールド詳細

| フィールド | 入力形式 | 必須 | デフォルト | 制限 |
|-----------|-----------|------|--------|------|
| 名前 | テキスト入力 | ○ | なし | 最大100文字 |
| 緯度 | 数値入力 | ○ | クリック位置 or 空 | -90〜90 |
| 経度 | 数値入力 | ○ | クリック位置 or 空 | -180〜180 |
| 画像 | ファイル選択/URL | × | なし | jpeg/png 最大5MB |
| 種別 | ドロップダウン選択 | × | なし | 5つの選択肢 |
| メモ | テキストエリア | × | なし | 最大500文字 |
| 登録先マップ | チェックボックス選択 | ○ | 現在のマップ | 複数選択可 |

---

### マップ上での表示

#### 表示アイコン

**カスタム地点のアイコン設定:**
- デフォルト: 青いピンアイコン
- 種別ごとにアイコン色・形状を変更:
  - 写真スポット: 青カメラアイコン
  - 待ち合わせ場所: 緑ピンアイコン
  - 景色の良い場所: 紫山アイコン
  - 記念碑・モニュメント: 茶色モニュメントアイコン
  - その他: 青いピンアイコン

**カフェとの区別:**
- カフェ: コーヒーカップアイコン
- カスタム地点: 種別ごとのアイコン

#### クリック時の動作

```
カスタム地点アイコンをクリック
   ↓
詳細情報ポップアップ表示 or サイドバー展開
   ↓
┌─────────────────────────────┐
│  東京タワー              [×] │
├─────────────────────────────┤
│  種別: 写真スポット           │
│                             │
│  緯度: 35.6890, 139.7020    │
│                             │
│  [画像を表示]                │
│                             │
│  メモ:                      │
│  夕日の時間帯が最も美しい       │
│                             │
│  登録日: 2025-11-30          │
│                             │
│  [詳細を編集] [地点を削除]     │
└─────────────────────────────┘
```

---

## 4. API仕様

### エンドポイント一覧

| メソッド | エンドポイント | 説明 |
|---------|---------------|------|
| POST | `/api/v1/custom-places/` | カスタム地点作成 |
| GET | `/api/v1/custom-places/` | カスタム地点一覧取得 |
| GET | `/api/v1/custom-places/{id}/` | カスタム地点詳細取得 |
| PUT | `/api/v1/custom-places/{id}/` | カスタム地点更新 |
| DELETE | `/api/v1/custom-places/{id}/` | カスタム地点削除 |
| GET | `/api/v1/maps/{map_id}/custom-places/` | マップ内のカスタム地点取得 |
| POST | `/api/v1/maps/{map_id}/custom-places/` | マップにカスタム地点を関連付け |
| DELETE | `/api/v1/maps/{map_id}/custom-places/{custom_place_id}/` | マップから関連を削除 |

---

### 4-1. カスタム地点作成

**エンドポイント:**
```
POST /api/v1/custom-places/
```

**リクエストボディ:**
```json
{
  "name": "東京タワー",
  "latitude": 35.6890,
  "longitude": 139.7020,
  "image": null,
  "place_type": "photo_spot",
  "memo": "夕日の時間帯が最も美しい",
  "map_ids": [1, 3]
}
```

**レスポンス（201 Created）:**
```json
{
  "id": 123,
  "owner": {
    "id": 1,
    "name": "山田太郎"
  },
  "name": "東京タワー",
  "latitude": 35.6890,
  "longitude": 139.7020,
  "image_url": null,
  "place_type": "photo_spot",
  "place_type_display": "写真スポット",
  "memo": "夕日の時間帯が最も美しい",
  "created_at": "2025-11-30T10:00:00Z",
  "updated_at": "2025-11-30T10:00:00Z",
  "maps": [
    {
      "id": 1,
      "name": "お気に入りカフェ"
    },
    {
      "id": 3,
      "name": "山田おすすめマップ"
    }
  ]
}
```

**エラーレスポンス（400 Bad Request）:**
```json
{
  "error": "バリデーションエラー",
  "details": {
    "name": ["この項目は必須です"],
    "latitude": ["緯度は-90から90の範囲で入力してください"]
  }
}
```

---

### 4-2. カスタム地点一覧取得

**エンドポイント:**
```
GET /api/v1/custom-places/
```

**クエリパラメータ:**
- `place_type`: 種別でフィルタ（例: `photo_spot`）
- `limit`: 取得件数の上限（デフォルト: 50）
- `offset`: ページネーション用のオフセット

**レスポンス（200 OK）:**
```json
{
  "count": 15,
  "next": "/api/v1/custom-places/?offset=50",
  "previous": null,
  "results": [
    {
      "id": 123,
      "name": "東京タワー",
      "latitude": 35.6890,
      "longitude": 139.7020,
      "image_url": "https://example.com/images/sunset.jpg",
      "place_type": "photo_spot",
      "place_type_display": "写真スポット",
      "created_at": "2025-11-30T10:00:00Z",
      "map_count": 2
    }
  ]
}
```

---

### 4-3. カスタム地点詳細取得

**エンドポイント:**
```
GET /api/v1/custom-places/{id}/
```

**レスポンス（200 OK）:**
```json
{
  "id": 123,
  "owner": {
    "id": 1,
    "name": "山田太郎"
  },
  "name": "東京タワー",
  "latitude": 35.6890,
  "longitude": 139.7020,
  "image_url": "https://example.com/images/sunset.jpg",
  "place_type": "photo_spot",
  "place_type_display": "写真スポット",
  "memo": "夕日の時間帯が最も美しい",
  "created_at": "2025-11-30T10:00:00Z",
  "updated_at": "2025-11-30T10:00:00Z",
  "maps": [
    {
      "id": 1,
      "name": "お気に入りカフェ",
      "is_visible": true
    }
  ]
}
```

---

### 4-4. カスタム地点更新

**エンドポイント:**
```
PUT /api/v1/custom-places/{id}/
```

**権限:** 所有者のみ更新可能

**リクエストボディ:**
```json
{
  "name": "東京タワー（更新）",
  "latitude": 35.6890,
  "longitude": 139.7020,
  "place_type": "viewpoint",
  "memo": "夕日の時間帯が最も美しい。16時頃がベスト"
}
```

**レスポンス（200 OK）:**
```json
{
  "id": 123,
  "name": "東京タワー（更新）",
  "latitude": 35.6890,
  "longitude": 139.7020,
  "place_type": "viewpoint",
  "place_type_display": "景色の良い場所",
  "memo": "夕日の時間帯が最も美しい。16時頃がベスト",
  "updated_at": "2025-11-30T11:00:00Z"
}
```

---

### 4-5. カスタム地点削除

**エンドポイント:**
```
DELETE /api/v1/custom-places/{id}/
```

**権限:** 所有者のみ削除可能

**レスポンス（204 No Content）**

**注意:**
- `CustomPlace` 本体が削除される
- 関連する `CustomPlaceMapRelation` も CASCADE で削除される

---

### 4-6. マップ内のカスタム地点取得

**エンドポイント:**
```
GET /api/v1/maps/{map_id}/custom-places/
```

**レスポンス（200 OK）:**
```json
{
  "map": {
    "id": 1,
    "name": "お気に入りカフェ"
  },
  "custom_places": [
    {
      "id": 123,
      "name": "東京タワー",
      "latitude": 35.6890,
      "longitude": 139.7020,
      "image_url": "https://example.com/images/sunset.jpg",
      "place_type": "photo_spot",
      "is_visible": true
    },
    {
      "id": 124,
      "name": "待ち合わせ場所",
      "latitude": 35.6812,
      "longitude": 139.7671,
      "image_url": null,
      "place_type": "meeting_point",
      "is_visible": true
    }
  ]
}
```

---

### 4-7. マップにカスタム地点を関連付け

**エンドポイント:**
```
POST /api/v1/maps/{map_id}/custom-places/
```

**リクエストボディ:**
```json
{
  "custom_place_id": 123
}
```

**レスポンス（201 Created）:**
```json
{
  "id": 456,
  "map_id": 1,
  "custom_place_id": 123,
  "is_visible": true,
  "created_at": "2025-11-30T10:00:00Z"
}
```

---

### 4-8. マップから関連を削除

**エンドポイント:**
```
DELETE /api/v1/maps/{map_id}/custom-places/{custom_place_id}/
```

**レスポンス（204 No Content）**

**注意:**
- `CustomPlaceMapRelation` のレコードのみ削除される
- カスタム地点本体は削除されない（他のマップで使用中の可能性があるため）

---

## 5. バリデーション

### フィールドレベルのバリデーション

#### 名前
- **必須項目**
- **最大100文字**
- **空白のみ不可**

#### 緯度
- **必須項目**
- **数値型**
- **-90 ≤ latitude ≤ 90**

#### 経度
- **必須項目**
- **数値型**
- **-180 ≤ longitude ≤ 180**

#### 画像
- **ファイル形式: jpeg, jpg, png のみ**
- **ファイルサイズ: 最大5MB**
- **URLも許可**: 有効なURL

#### 種別（place_type）
- **許可値**: `photo_spot`, `meeting_point`, `viewpoint`, `memorial`, `other`
- **未選択も許可**

#### メモ
- **最大500文字**
- **任意項目**

---

### バリデーション実装（Django）

```python
class CustomPlaceSerializer(serializers.ModelSerializer):
    """カスタム地点シリアライザ"""

    def validate_name(self, value):
        if not value or not value.strip():
            raise serializers.ValidationError("名前は必須です")
        if len(value) > 100:
            raise serializers.ValidationError("名前は100文字以内で入力してください")
        return value.strip()

    def validate_latitude(self, value):
        if value < -90 or value > 90:
            raise serializers.ValidationError("緯度は-90から90の範囲で入力してください")
        return value

    def validate_longitude(self, value):
        if value < -180 or value > 180:
            raise serializers.ValidationError("経度は-180から180の範囲で入力してください")
        return value

    def validate_image(self, value):
        if value:
            # ファイルサイズ検証
            if value.size > 5 * 1024 * 1024:  # 5MB
                raise serializers.ValidationError("画像のサイズは5MB以下にしてください")

            # ファイル形式検証
            allowed_extensions = ['jpg', 'jpeg', 'png']
            ext = value.name.split('.')[-1].lower()
            if ext not in allowed_extensions:
                raise serializers.ValidationError("画像はjpeg, jpg, pngのいずれかで選択してください")

        return value

    def validate_memo(self, value):
        if value and len(value) > 500:
            raise serializers.ValidationError("メモは500文字以内で入力してください")
        return value
```

---

## 6. バックエンド実装

### Django Views

```python
from rest_framework import viewsets, status
from rest_framework.decorators import action
from rest_framework.response import Response
from rest_framework.permissions import IsAuthenticated
from .models import CustomPlace, CustomPlaceMapRelation
from .serializers import CustomPlaceSerializer

class CustomPlaceViewSet(viewsets.ModelViewSet):
    """カスタム地点 ViewSet"""

    permission_classes = [IsAuthenticated]
    serializer_class = CustomPlaceSerializer

    def get_queryset(self):
        # 自分が作成したカスタム地点のみ取得
        return CustomPlace.objects.filter(owner=self.request.user)

    def perform_create(self, serializer):
        # 所有者を自動設定
        custom_place = serializer.save(owner=self.request.user)

        # map_ids が指定されていればマップに関連付け
        map_ids = self.request.data.get('map_ids', [])
        for map_id in map_ids:
            try:
                map_obj = Map.objects.get(id=map_id)
                # マップへのアクセス権限を確認
                if MapUserRelation.objects.filter(
                    map=map_obj,
                    user=self.request.user
                ).exists():
                    CustomPlaceMapRelation.objects.create(
                        map=map_obj,
                        custom_place=custom_place
                    )
            except Map.DoesNotExist:
                pass

    def perform_update(self, serializer):
        # 所有者チェックは get_queryset でフィルタ済み
        serializer.save()

    def perform_destroy(self, instance):
        # 所有者チェックは get_queryset でフィルタ済み
        instance.delete()

    @action(detail=False, methods=['get'])
    def by_map(self, request, map_id=None):
        """マップ内のカスタム地点取得"""
        try:
            map_obj = Map.objects.get(id=map_id)

            # マップへのアクセス権限を確認
            if not MapUserRelation.objects.filter(
                map=map_obj,
                user=request.user
            ).exists():
                return Response(
                    {"error": "このマップへのアクセス権限がありません"},
                    status=status.HTTP_403_FORBIDDEN
                )

            # マップに関連するカスタム地点を取得
            relations = CustomPlaceMapRelation.objects.filter(
                map=map_obj
            ).select_related('custom_place')

            custom_places = [rel.custom_place for rel in relations]
            serializer = self.get_serializer(custom_places, many=True)

            return Response({
                'map': {
                    'id': map_obj.id,
                    'name': map_obj.name
                },
                'custom_places': serializer.data
            })

        except Map.DoesNotExist:
            return Response(
                {"error": "マップが見つかりません"},
                status=status.HTTP_404_NOT_FOUND
            )
```

---

### フロントエンド実装（React + TypeScript）

#### customPlaceService.ts

```typescript
import { API_BASE_PATH } from "../constants/api";

export interface CustomPlace {
  id: number;
  owner: {
    id: number;
    name: string;
  };
  name: string;
  latitude: number;
  longitude: number;
  image_url?: string;
  place_type?: string;
  place_type_display?: string;
  memo?: string;
  created_at: string;
  updated_at: string;
  maps?: {
    id: number;
    name: string;
    is_visible: boolean;
  }[];
}

export interface CreateCustomPlaceRequest {
  name: string;
  latitude: number;
  longitude: number;
  image?: File | null;
  place_type?: string;
  memo?: string;
  map_ids?: number[];
}

/**
 * CSRFトークン取得
 */
const getCsrfToken = async (): Promise<string> => {
  const response = await fetch(`${API_BASE_PATH}/csrf/`, {
    credentials: "include",
  });
  const data = await response.json();
  return data.csrfToken;
};

/**
 * カスタム地点を作成
 */
export const createCustomPlace = async (
  data: CreateCustomPlaceRequest
): Promise<CustomPlace> => {
  const csrfToken = await getCsrfToken();

  const formData = new FormData();
  formData.append('name', data.name);
  formData.append('latitude', data.latitude.toString());
  formData.append('longitude', data.longitude.toString());

  if (data.image) {
    formData.append('image', data.image);
  }
  if (data.place_type) {
    formData.append('place_type', data.place_type);
  }
  if (data.memo) {
    formData.append('memo', data.memo);
  }
  if (data.map_ids) {
    formData.append('map_ids', JSON.stringify(data.map_ids));
  }

  const response = await fetch(`${API_BASE_PATH}/custom-places/`, {
    method: 'POST',
    credentials: 'include',
    headers: {
      'X-CSRFToken': csrfToken,
    },
    body: formData,
  });

  if (!response.ok) {
    const errorData = await response.json().catch(() => ({}));
    throw new Error(errorData.error || 'カスタム地点の作成に失敗しました');
  }

  return response.json();
};

/**
 * カスタム地点一覧を取得
 */
export const getCustomPlaces = async (): Promise<{
  count: number;
  results: CustomPlace[];
}> => {
  const response = await fetch(`${API_BASE_PATH}/custom-places/`, {
    method: 'GET',
    credentials: 'include',
    headers: {
      'Content-Type': 'application/json',
    },
  });

  if (!response.ok) {
    throw new Error('カスタム地点の取得に失敗しました');
  }

  return response.json();
};

/**
 * マップ内のカスタム地点を取得
 */
export const getCustomPlacesByMap = async (mapId: number): Promise<{
  map: { id: number; name: string };
  custom_places: CustomPlace[];
}> => {
  const response = await fetch(
    `${API_BASE_PATH}/maps/${mapId}/custom-places/`,
    {
      method: 'GET',
      credentials: 'include',
      headers: {
        'Content-Type': 'application/json',
      },
    }
  );

  if (!response.ok) {
    throw new Error('カスタム地点の取得に失敗しました');
  }

  return response.json();
};

/**
 * カスタム地点を更新
 */
export const updateCustomPlace = async (
  id: number,
  data: Partial<CreateCustomPlaceRequest>
): Promise<CustomPlace> => {
  const csrfToken = await getCsrfToken();

  const response = await fetch(`${API_BASE_PATH}/custom-places/${id}/`, {
    method: 'PUT',
    credentials: 'include',
    headers: {
      'Content-Type': 'application/json',
      'X-CSRFToken': csrfToken,
    },
    body: JSON.stringify(data),
  });

  if (!response.ok) {
    const errorData = await response.json().catch(() => ({}));
    throw new Error(errorData.error || 'カスタム地点の更新に失敗しました');
  }

  return response.json();
};

/**
 * カスタム地点を削除
 */
export const deleteCustomPlace = async (id: number): Promise<void> => {
  const csrfToken = await getCsrfToken();

  const response = await fetch(`${API_BASE_PATH}/custom-places/${id}/`, {
    method: 'DELETE',
    credentials: 'include',
    headers: {
      'X-CSRFToken': csrfToken,
    },
  });

  if (!response.ok) {
    throw new Error('カスタム地点の削除に失敗しました');
  }
};
```

---

## 7. シーケンス図

### シーケンス1: マップをクリックして登録

```
ユーザー     フロントエンド        バックエンド       データベース

     │  地点クリック  →
     │
     │            ← 座標取得      →
     │
     │  ←  入力フォーム表示
     │
     │  データ入力   →
     │
     │              POST /custom-places/
     │                            ┌──→ INSERT        →
     │                            └←── Success
     │
     │                            ┌──→ INSERT relation
     │                            └←── Success
     │              ←  201 Created
     │
     │  ←  完了通知
     │
     │  ←  マップに地点表示
     │
```

---

## 8. 将来の拡張案

### 1. 統合API対応（フェーズ2）

**目的:** Cafe と CustomPlace を統一的に扱う

**エンドポイント:**
```
GET /api/v1/maps/{map_id}/points/
```

**レスポンス:**
```json
{
  "points": [
    {
      "type": "cafe",
      "id": 1,
      "name": "スターバックスコーヒー",
      "latitude": 35.6590,
      "longitude": 139.7004,
      "icon": "cafe"
    },
    {
      "type": "custom_place",
      "id": 123,
      "name": "東京タワー",
      "latitude": 35.6890,
      "longitude": 139.7020,
      "icon": "photo_spot"
    }
  ]
}
```

---

### 2. 公開機能（フェーズ3）

- ユーザー間でカスタム地点を共有
- 公開レベルの設定（全体公開・限定公開）
- 公開時に必要な情報の追加

---

### 3. 画像管理機能（フェーズ3）

- 1つの地点に複数画像を登録
- 画像のサムネイル生成
- 画像の並び替え・削除

---

### 4. 検索機能（フェーズ3）

- カスタム地点の全文検索
- 種別やタグでのフィルタリング
- 地図範囲内の検索

---

### 5. インポート/エクスポート機能（フェーズ3）

- CSVやJSONでの一括登録
- データのバックアップ
- 他サービスとの連携

---

### 6. 統計機能（フェーズ3）

- カスタム地点の登録数
- 種別ごとの統計
- マップごとの利用状況

---

## 9. 技術スタック

### フロントエンド
- **UI Framework:** React + TypeScript
- **地図ライブラリ:** Google Maps JavaScript API
- **状態管理:** React useState / Context API
- **HTTPクライアント:** Fetch API
- **画像アップロード:** FormData API

### バックエンド
- **Webフレームワーク:** Django 5.1.7
- **REST API:** Django REST Framework
- **データベース:** PostgreSQL
- **認証:** Session Authentication + CSRF Protection
- **画像ストレージ:** Django ImageField（ローカルストレージ or S3）
- **バリデーション:** DRF Serializers

### インフラ
- **画像保存先:** ローカルファイルシステム（開発）/ AWS S3（本番）
- **データベース:** PostgreSQL
- **ホスティング:** AWS EC2
- **静的ファイル配信:** Nginx（将来的に）

---

## 10. 変更履歴

| 日付 | バージョン | 変更内容 |
|------|-----------|---------|
| 2025-11-30 | 1.0.0 | 初版 - 基本仕様書作成 |

---

## 11. 参考資料

- カスタム地点登録機能の実装はこの仕様書に基づいて行う
- 実装状況に応じて将来の拡張案を随時更新する
- API仕様変更時は必ずこの仕様書を更新する

---

## 12. 補足事項

### セキュリティ対策

1. **権限チェック:**
   - カスタム地点の作成・削除は所有者のみ可能
   - マップへの関連付けはマップへのアクセス権限が必要

2. **画像アップロード:**
   - ファイルサイズ上限（5MB）
   - 許可する拡張子の制限（jpeg, png）
   - XSS対策: ファイル名のサニタイズ

3. **位置情報:**
   - 緯度・経度の妥当性チェック
   - 不正な座標値の排除

### パフォーマンス対策

1. **データベースインデックス:**
   - `owner_id` + `created_at` のインデックス作成
   - `latitude` + `longitude` のインデックス作成（将来的な範囲検索用）

2. **画像最適化:**
   - アップロード時にサムネイル生成
   - 圧縮処理の実施

3. **キャッシュ戦略:**
   - マップ別の地点一覧をキャッシュ
   - Redis でキャッシュ管理（将来的に）

---

## 13. 関連ドキュメント

- [カフェ検索機能.md](./カフェ検索機能.md)
- [カスタマイズ機能.md](./カスタマイズ機能.md)
- [チャット機能.md](./チャット機能.md)
